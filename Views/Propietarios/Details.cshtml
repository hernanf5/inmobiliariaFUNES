@model inmobiliariaFUNES.Models.Propietario;

@{
    ViewData["Title"] = "Detalle de propietario";
}

<h1 class="text-2xl font-semibold text-gray-900 mb-6">@Model.Nombre @Model.Apellido</h1>

<div class="bg-white border border-gray-200 rounded-lg p-6 max-w-xl divide-y divide-gray-100">
    <div class="py-3 flex justify-between text-sm">
        <span class="text-gray-500">DNI/CUIT</span>
        <span class="text-gray-900">@Model.DniCuit</span>
    </div>
    <div class="py-3 flex justify-between text-sm">
        <span class="text-gray-500">Teléfono</span>
        <span class="text-gray-900">@Model.Telefono</span>
    </div>
    <div class="py-3 flex justify-between text-sm">
        <span class="text-gray-500">Email</span>
        <span class="text-gray-900">@Model.Email</span>
    </div>
</div>

<div class="mt-6 flex items-center justify-between">
    <a asp-action="Index" class="text-sm text-indigo-600 hover:text-indigo-800">&larr; Volver al listado</a>
    <a asp-controller="Inmuebles" asp-action="Index" asp-route-idPropietario="@Model.IdPropietario" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-medium text-white hover:bg-indigo-700">
        Ver inmuebles de este propietario
    </a>
</div>